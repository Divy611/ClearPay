enum AuthStatus {
  NOT_DETERMINED,
  NOT_LOGGED_IN,
  LOGGED_IN,
}

enum RewardTier {
  bronze,
  silver,
  gold,
  platinum,
}

const USERS = 'Users';
const REQUESTS = 'Requests';
const TRANSACTIONS = 'Transactions';
